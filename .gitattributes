# Default: treat text files as text and normalize to LF in the repo
*               text=auto eol=lf

# Common binary types (never touch their line endings)
*.png           -text
*.jpg           -text
*.jpeg          -text
*.gif           -text
*.webp          -text
*.ico           -text
*.pdf           -text
*.zip           -text

# Platform-specific scripts
*.sh            text eol=lf
*.bash          text eol=lf
*.ps1           text eol=crlf
*.bat           text eol=crlf